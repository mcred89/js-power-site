version: 0.2
phases:
  install:
    commands:
      - cd static
      - npm install
  pre_build:
    commands:
      - echo "pre_build"
  build:
    commands:
      - npm run-script build
  post_build:
    commands:
      - echo "post_build"