version: 0.2
phases:
  install:
    commands:
      - npm install -g serverless
  pre_build:
    commands:
      - echo "pre_build"
  build:
    commands:
      - cd static
      - serverless deploy --stage prod
  post_build:
    commands:
      - echo "post_build"