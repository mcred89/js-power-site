import React, { useState } from 'react';
import { importPlanSummary } from '../data/importBackup';
import { canShareTransfer, createSharedTransferContents, download } from '../data/transferUi';

export const TrackerNotices = ({ message, updateRegistration }) => {
  const [updating, setUpdating] = useState(false);
  const activateUpdate = async () => {
    if (updating) return;
    setUpdating(true);
    let currentRegistration = null;
    try {
      currentRegistration = await navigator.serviceWorker?.getRegistration?.();
    } catch {
      // The registration passed by the observer remains a safe fallback.
    }
    const waitingWorker = currentRegistration?.waiting || updateRegistration?.waiting;
    if (!waitingWorker) {
      setUpdating(false);
      return;
    }
    try {
      waitingWorker.postMessage('skip-waiting');
    } catch {
      setUpdating(false);
    }
  };
  return <>{message && <div className="toast" role="status">{message}</div>}{updateRegistration && <div className="update-banner"><span>A new version is ready.</span><button type="button" disabled={updating} onClick={activateUpdate}>{updating ? 'Updating…' : 'Update now'}</button></div>}</>;
};

export const ImportPreview = ({ plan, busy, onCancel, onConfirm }) => {
  const summary = importPlanSummary(plan);
  const sections = [['Profiles', plan.profiles], ['Routines', plan.routines], ['Templates', plan.templates || []]];
  return <div className="modal-backdrop"><section className="confirmation-modal import-preview" role="dialog" aria-modal="true" aria-labelledby="import-preview-title"><p className="eyebrow">Backup review</p><h2 id="import-preview-title">Preview import</h2><p>{summary.copy} copied · {summary.skip} skipped · {summary.merge} merged</p><p className="import-note">Merges keep this phone's values and completed workout snapshots, while adding records and workouts found only in the backup.</p><div className="import-preview-list">{sections.map(([title, items]) => <div key={title}><h3>{title}</h3>{!items.length ? <p>None</p> : items.map((item, index) => <div className="import-preview-row" key={`${item.imported.id}-${index}`}><span><strong>{item.imported.name || 'Unnamed'}</strong><small>{item.status}</small></span><b className={`import-action ${item.action}`}>{item.action}</b></div>)}</div>)}</div><div className="button-row modal-actions"><button className="secondary-button" type="button" disabled={busy} onClick={onCancel}>Cancel</button><button className="primary-button" type="button" disabled={busy} onClick={onConfirm}>{busy ? 'Importing…' : 'Import backup'}</button></div></section></div>;
};

export const TransferCreator = ({ transfer, onClose, onShare }) => {
  const shareSupported = canShareTransfer(transfer);
  return <div className="modal-backdrop"><section className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="transfer-title"><p className="eyebrow">Encrypted transfer</p><h2 id="transfer-title">Send to another phone</h2><p>{shareSupported ? 'Use your phone\'s share menu and choose Quick Share or another nearby option.' : 'Direct sharing is not supported by this browser. Download the file and share it from your phone\'s Files app.'}</p>{shareSupported && <button className="primary-button full-button" type="button" onClick={onShare}>Share with nearby phone</button>}<button className="secondary-button full-button" type="button" onClick={() => download(createSharedTransferContents(transfer), transfer.filename, 'text/plain')}>Download file instead</button><p>On the other phone, accept the file and open it with McIlroy Method. The app will preview it automatically. It expires at {new Date(transfer.expiresAt).toLocaleTimeString([], { hour: 'numeric', minute: '2-digit' })}.</p><div className="button-row modal-actions"><button className="primary-button" type="button" onClick={onClose}>Done</button></div></section></div>;
};

export const TransferUnlock = ({ file, busy, onCancel, onUnlock }) => { const [key, setKey] = useState(''); return <div className="modal-backdrop"><form className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="unlock-title" onSubmit={event => { event.preventDefault(); if (!busy) onUnlock(key); }}><p className="eyebrow">Encrypted transfer</p><h2 id="unlock-title">Open transfer package</h2><p>{file.name} needs the key shown on the sending device.</p><label className="form-field"><span className="field-label">Transfer key</span><input className="number-input transfer-key-input" disabled={busy} value={key} onChange={event => setKey(event.target.value)} autoCapitalize="none" autoCorrect="off" required autoFocus /></label>{busy && <p role="status">Decrypting transfer…</p>}<div className="button-row modal-actions"><button className="secondary-button" type="button" onClick={onCancel}>Cancel</button><button className="primary-button" type="submit" disabled={busy}>{busy ? 'Decrypting…' : 'Decrypt and preview'}</button></div></form></div>; };

export const RoutineTransferCreator = ({ routines, busy, onCancel, onCreate }) => { const [routineId, setRoutineId] = useState(routines[0]?.id || ''); return <div className="modal-backdrop"><form className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="routine-transfer-title" onSubmit={event => event.preventDefault()}><p className="eyebrow">Routine transfer</p><h2 id="routine-transfer-title">Choose a routine</h2><p>The encrypted routine and its history can be sent through your phone's share menu.</p><label className="form-field"><span className="field-label">Routine</span><select className="number-input" disabled={busy} value={routineId} onChange={event => setRoutineId(event.target.value)}>{routines.map(item => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label>{busy && <p role="status">Encrypting transfer…</p>}<div className="button-row modal-actions"><button className="text-button" type="button" onClick={onCancel}>Cancel</button><button className="primary-button" type="button" disabled={!routineId || busy} onClick={() => onCreate(routineId)}>{busy ? 'Creating…' : 'Create transfer'}</button></div></form></div>; };

export const RoutineDestination = ({ transfer, profiles, onCancel, onConfirm }) => { const [destination, setDestination] = useState(profiles[0]?.id || 'new'); const [name, setName] = useState(transfer.profileName || 'Imported profile'); return <div className="modal-backdrop"><form className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="routine-destination-title" onSubmit={event => { event.preventDefault(); onConfirm(destination, name.trim()); }}><p className="eyebrow">Routine received</p><h2 id="routine-destination-title">Where should {transfer.routine.name} go?</h2><label className="form-field"><span className="field-label">Profile</span><select className="number-input" value={destination} onChange={event => setDestination(event.target.value)}>{profiles.map(item => <option value={item.id} key={item.id}>Add to {item.name}</option>)}<option value="new">Create a new profile</option></select></label>{destination === 'new' && <label className="form-field"><span className="field-label">New profile name</span><input className="number-input" value={name} onChange={event => setName(event.target.value)} required autoFocus /></label>}<div className="button-row modal-actions"><button className="secondary-button" type="button" onClick={onCancel}>Cancel</button><button className="primary-button" type="submit">Preview import</button></div></form></div>; };

export const RoutineCopyDialog = ({ title, eyebrow, defaultName, profiles, selectedProfileId, confirmLabel, onCancel, onConfirm }) => { const [destination, setDestination] = useState(selectedProfileId || profiles[0]?.id || ''); const [name, setName] = useState(defaultName); const trimmedName = name.trim(); return <div className="modal-backdrop"><form className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="copy-routine-title" onSubmit={event => { event.preventDefault(); if (destination && trimmedName) onConfirm(destination, trimmedName); }}><p className="eyebrow">{eyebrow}</p><h2 id="copy-routine-title">{title}</h2><label className="form-field"><span className="field-label">Routine name</span><input aria-label="Routine name" className="number-input" value={name} onChange={event => setName(event.target.value)} required autoFocus /></label><label className="form-field"><span className="field-label">Profile</span><select aria-label="Destination profile" className="number-input" value={destination} onChange={event => setDestination(event.target.value)} required>{profiles.map(item => <option value={item.id} key={item.id}>{item.name}</option>)}</select></label><div className="button-row modal-actions"><button className="secondary-button" type="button" onClick={onCancel}>Cancel</button><button className="primary-button" type="submit" disabled={!destination || !trimmedName}>{confirmLabel}</button></div></form></div>; };

export const SaveTemplateDialog = ({ routine, onCancel, onConfirm }) => { const [name, setName] = useState(routine.name); const trimmedName = name.trim(); return <div className="modal-backdrop"><form className="confirmation-modal" role="dialog" aria-modal="true" aria-labelledby="save-template-title" onSubmit={event => { event.preventDefault(); if (trimmedName) onConfirm(trimmedName); }}><p className="eyebrow">Reusable template</p><h2 id="save-template-title">Save routine setup</h2><p>Templates keep the generator setup, not workout progress or exercise-level edits.</p><label className="form-field"><span className="field-label">Template name</span><input aria-label="Template name" className="number-input" value={name} onChange={event => setName(event.target.value)} required autoFocus /></label><div className="button-row modal-actions"><button className="secondary-button" type="button" onClick={onCancel}>Cancel</button><button className="primary-button" type="submit" disabled={!trimmedName}>Save template</button></div></form></div>; };
